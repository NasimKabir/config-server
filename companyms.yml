server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/company?useSSL=false&serverTimezone=UTC
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 1011
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
